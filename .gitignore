node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local agent/editor tooling
.claude/

# Capacitor native projects — generated by `npx cap add <platform>`.
# Regenerate rather than commit; all configuration lives in capacitor.config.ts.
/android/
/ios/

# Firebase — .firebaserc holds a project id, so it stays local.
.firebaserc
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Service account keys grant full admin access and bypass every security rule.
# Never commit one; keep it outside the repo and reference it by path.
*firebase-adminsdk*.json
service-account*.json
serviceAccountKey*.json
**/*-adminsdk-*.json
